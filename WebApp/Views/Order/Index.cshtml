@using WebApp.Common
@model IEnumerable<WebApp.Models.CustomizeOrder>
@{
    ViewBag.Title = "Order";
}

@section Styles{
    @Styles.Render("~/Content/AdminCss")

    <!----webfonts--->
    <link href='//fonts.googleapis.com/css?family=Cabin:400,400italic,500,500italic,600,600italic,700,700italic' rel='stylesheet' type='text/css'>
    <!---//webfonts--->
    <!-- Meters graphs -->
    <!-- Placed js at the end of the document so the pages load faster -->
}

@section Scripts{
    <script type="application/x-javascript"> addEventListener("load", function() { setTimeout(hideURLbar, 0); }, false); function hideURLbar(){ window.scrollTo(0,1); } </script>

    <!-- chart -->
    <script src="~/Content/Admin/js/Chart.js"></script>
    <!-- //chart -->
    <script src="~/Content/Admin/js/wow.min.js"></script>

    <!--//end-animate-->
    <script src="~/Content/Admin/js/jquery-1.10.2.min.js"></script>
    <script>
        new WOW().init();
    </script>
    <script type="text/javascript" charset="utf8" src="~/Scripts/media/js/jquery.dataTables.js"></script>

}

@section JsFooter{
    <script type="text/javascript">
        $(document)
            .ready(function() {
                $('#tblAccount')
                    .DataTable({
                        responsive: true
                    });
            });
    </script>
    <script src="~/Content/Admin/js/jquery.nicescroll.js"></script>
    <script src="~/Content/Admin/js/scripts.js"></script>
    <!-- Bootstrap Core JavaScript -->
    <script src="~/Content/Admin/js/bootstrap.min.js"></script>
}

<div id="page-wrapper">
    <div class="panel panel-primary">
        <div class="panel-heading">
            <h3>Account Manage</h3>
        </div>
        <div class="panel-body">
            <div class="dataTable_wrapper">
                <div class="responsive-table">
                    <table id="tblAccount" class="table table-striped table-hover">
                        <thead>
                            <tr>
                                <th class="info">BUYER</th>
                                <th class="info">ORDER DATE</th>
                                <th class="info">DELIVERY DATE</th>
                                <th class="info">TOTAL</th>
                                <th class="info"></th>
                                <th class="info"></th>
                            </tr>
                        </thead>
                        <tbody>
                            @foreach (var item in Model)
                            {
                                <tr>
                                    <td id="buyerName-@item.Order.id" data-buyername="@item.Username">@item.Username</td>
                                    <td id="email-@item.Order.id">@(item.Order.orderDate.ToShortDateString())</td>
                                    <td id="address-@item.Order.id">@(null != item.Order.deliveryDate ? item.Order.deliveryDate.ToString() : "PENDING")</td>
                                    <td id="total-@item.Order.id" data-total="@item.Order.total">@(item.Order.total.ToString("N0") + " VND")</td>


                                    @*<td>
                                            <a href="#" class="btn btn-primary btn-block btn-admin" data-id="@item.id">@(role == Constant.AdminRole ? "UNADMIN" : "ADMIN")</a>
                                        </td>*@
                                    @{
                                        var url = "/OrderDetail/ViewDetail?orderId=" + item.Order.id + "&buyerName=" + item.Username + "&total=" + item.Order.total;
                                    }
                                    <td>
                                        <a href="@url" class="btn btn-warning btn-block btn-viewdetail" data-id="@item.Order.id">VIEW DETAIL</a>
                                    </td>
                                    <td id="id-@item.Order.id">@item.Order.buyerId</td>
                                </tr>

                                        }

                        </tbody>
                    </table>
                </div>
            </div>
        </div>
    </div>
</div>
